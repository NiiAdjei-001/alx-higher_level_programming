#!/usr/bin/python3
def number_keys(a_dictionary):
    """Gets the number of keys of a dictionry

        Args:
        a_dictionary: A dictionary

        Returns:
        Returns the number of keys in a dictionary
    """
    return len(list(a_dictionary))
